# Study Plan + Dictation 功能补丁

本补丁只修改 `main/ui.c` 中用户指定的两个功能。

## 1. Study Plan
- 不再只显示 `Plan ready`。
- 可使用 `-5 / +5` 调整每日单词目标。
- 范围 5~100。
- 点击 `SAVE` 后调用现有 `vocab_set_daily_target()` 保存到 NVS。
- 页面显示今日完成数、待复习数、当前已保存目标和保存结果。

## 2. Dictation
- 显示当前单词的中文释义作为提示，不直接泄露英文答案。
- 白色区域保留真实触摸手写轨迹。
- 修复 1-bit indexed canvas 调色板，确保黑色笔迹可见。
- 每次手写一个大写 A-Z 字母后点 `ADD`。
- ESP32 本地 5x7 轻量识别器将识别结果追加到上方单词框。
- `DEL` 删除最后一个已识别字母；`CLEAR` 只清空当前手写区。
- `CHECK` 比较你看到的已识别单词：
  - 正确：显示“正确！你写的是……”
  - 错误：显示“你写的是…… 正确：……”
- 第一次 CHECK 判定并保存学习进度，按钮变为 NEXT；第二次进入下一题。

识别器面向逐个、清晰的“大写印刷体英文字母”，不是连续英文草写 OCR。
